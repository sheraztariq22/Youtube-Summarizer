# ============================================================================
# ENVIRONMENT VARIABLES FOR YOUTUBE VIDEO SUMMARIZER
# ============================================================================
# 
# Instructions:
# 1. Copy this file and rename it to ".env" (without .example)
# 2. Fill in your actual API key below
# 3. Never commit the .env file to version control (add it to .gitignore)
#
# Get your free Gemini API key from:
# https://makersuite.google.com/app/apikey
# ============================================================================

# Google Gemini API Key (Required)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Gemini Model Selection (default: gemini-1.5-flash-latest)
# Options: gemini-1.5-flash-latest, gemini-1.5-pro-latest, gemini-pro
GEMINI_MODEL=gemini-1.5-flash-latest

# Optional: Gradio Server Configuration
GRADIO_SERVER_NAME=0.0.0.0
GRADIO_SERVER_PORT=7860

# Optional: Embedding Model (default: all-MiniLM-L6-v2)
EMBEDDING_MODEL=all-MiniLM-L6-v2